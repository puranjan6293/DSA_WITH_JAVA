package Leetcode;

public class preOrder_Traversal_Tree {
    /*
    public List<Integer> preorderTraversal(TreeNode root) {
        List<Integer> ans = new ArrayList<>();
        preOrder(root, ans);
        return ans;
    }
    static void preOrder(TreeNode root, List<Integer> lis){
        if(root==null) return;
        lis.add(root.val);
        preOrder(root.left, lis);
        preOrder(root.right, lis);
    }
     */
}
