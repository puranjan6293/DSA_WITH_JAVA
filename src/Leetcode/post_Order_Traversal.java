package Leetcode;

public class post_Order_Traversal {
    /*
    public List<Integer> postorderTraversal(TreeNode root) {
        List<Integer> ans = new ArrayList<>();
        postOrder(root, ans);
        return ans;
    }
    static void postOrder(TreeNode root, List<Integer> lis){
        if(root==null) return;
        postOrder(root.left, lis);
        postOrder(root.right, lis);
        lis.add(root.val);
    }
     */
}
